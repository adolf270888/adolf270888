name: messenger_client_flutter
description: Flutter web/windows client for MessengerServer
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.4.3+1
  shared_preferences: ^2.2.3
  signalr_netcore: ^1.4.3
  file_picker: ^8.0.0+1

flutter:
  uses-material-design: true
